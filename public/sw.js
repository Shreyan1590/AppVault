
// A basic service worker for PWA functionality
self.addEventListener('fetch', (event) => {
  // We are not adding any specific caching logic here for now.
  // This file is primarily to make the web app installable.
});
